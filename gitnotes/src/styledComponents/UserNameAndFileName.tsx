import { styled } from "@mui/material/styles";
export const UserNameAndFileName = styled("div")(() => ({
    display: "flex",
    flexDirection: "row",
    color:'blue',
    fontSize:"1em"

  }));
  export  const PlainText = styled("div")(() => ({
    display: "flex",
   
    color:'grey',
    fontSize:"0.9em"
  }));